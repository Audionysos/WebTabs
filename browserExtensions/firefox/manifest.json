{
	"manifest_version": 2,
	"name": "Tabs to file saver",
	"description": "Saves all tabs urls of current browser's window to a file",
	"version": "1.0",
	"permissions": ["<all_urls>", "tabs", "downloads"],
	"background": {
		"scripts": ["background.js"]
	},
	"browser_action": {
		"default_title": "Save tabs" 
	}
}